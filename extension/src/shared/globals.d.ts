// Injected by build.mjs's esbuild `define` at build time — never assigned
// to at runtime, only ever read. See src/shared/config.ts.
declare const __SERVER_BASE_URL__: string;
declare const __JITSI_DOMAIN__: string;
